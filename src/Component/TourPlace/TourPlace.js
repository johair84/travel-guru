import React from 'react';

const TourPlace = (props) => {
    console.log(props.tourPlace.key);
    return (
        <div>
            <h1>This Cox bazar detalis</h1>
        </div>
    );
};

export default TourPlace;